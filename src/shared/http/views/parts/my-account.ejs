<!-- Title -->
<title>
  Minha Agenda | Minha conta
</title>
</head>
<body>
  <div id="room" class="content">
    <header>
      <a href="/">
        <img src="/images/logo.svg" alt="Minha Agenda Logo" />
      </a>
      <div class="buttons">
        <p>Olá, <%= user.name %>!</p>
        <a href="/logout" class="button transparent">
          <img src="/images/logout.svg" alt="Sair" />
          Sair
        </a>
      </div>
    </header>
    <main>
      <section id="question-form">
        <div>
          <% if (msgType==="error" && msgContent) { %>
          <div class="message-wrapper error" id="message-wrapper">
            <div class="message-content">
              <p>
                <img src="/images/error.svg" alt="Warning" />
                <%= msgContent; %>
              </p>
              <img src="/images/close.svg" alt="Close" class="close-error" id="close-error" />
            </div>
          </div>
          <% }; %> <% if (msgType==="success" && msgContent) { %>
          <div class="message-wrapper success" id="message-wrapper">
            <div class="message-content">
              <p>
                <img src="/images/success.svg" alt="Success" />
                <%= msgContent; %>
              </p>
              <img src="/images/close.svg" alt="Warning" class="close-error" id="close-error" />
            </div>
          </div>
          <% }; %>
        </div>

        <%- include(`my-contacts.ejs`); %>

      </section>
    </main>
  </div>
  <!-- MODAL (Add Contact) -->
  <%- include(`modal-add.ejs`); %>
  <!-- MODAL (Add Contact) -->

  <!-- MODAL (Edit Contact) -->
  <%- include(`modal-edit.ejs`); %>
  <!-- MODAL (Edit Contact) -->

  <!-- MODAL (Delete Contact) -->
  <%- include(`modal-delete.ejs`); %>
  <!-- MODAL (Delete Contact) -->
</body>
